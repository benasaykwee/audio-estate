# ============================================================================
# DEAD FILE — kept only because the sandbox that emptied it cannot delete
# files on this volume. Safe to `rm -rf CASKET/.github` at any time.
#
# GitHub reads workflows from <repo-root>/.github/workflows and nowhere else.
# The repository is rooted at the CLAUDE folder, so CASKET's real workflow
# lives at:
#
#     .github/workflows/casket.yml
#
# It runs with `defaults.run.working-directory: CASKET`, which is what lets a
# job that starts at the estate root behave as though it started inside the
# project. The root copy also carries five fixes this one never had: the
# auval manufacturer code, the AUTOPSY tripwire's escaped `cd`, the soak's
# impossible timeout, the artefact filename casing, and the offline-tools
# fuzzer's runtime.
#
# This file previously held a full copy. Two copies of a CI definition is one
# CI definition and one lie: nothing runs this one, so nothing tells you when
# it stops matching the one that does.
#
# Checked by `node tools/estate_lint.js`.
# ============================================================================
